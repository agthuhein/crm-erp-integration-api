server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/leave_app_db
    username: root
    password: root12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  main:
    web-application-type: servlet
